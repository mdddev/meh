{
  "devDependencies": {
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^8.0.4",
    "husky": "^7.0.0",
    "lint-staged": "^12.3.8",
    "semantic-release": "^19.0.2"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.cs": "dotnet format --no-restore --verify-no-changes --include"
  }
}
